Placeholder wordmarks for the top banner (yale.svg, biohub.svg).
These are simple stand-ins, NOT the official trademarked logos.
To use the real institutional logos, replace these two files with the
official Yale and CZ Biohub logo assets (keep the same filenames, and
use white/knockout versions since the banner is black). SVG or PNG both
work — if you use PNG, update the <img src> extension in index.html.
